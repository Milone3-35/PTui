The whole screen is the size and the things that
are getting saved is from the highest, lowest, most right, and most left points,
kind a square "drawn" form there. not really but yeah. 
Then make the tuit exportable and importable.
After pressing backspace, the highest points could be outdated, so i should save the 
position of them and if cursor_pos matches it then we restore it to a const, maybe PHP_INT_MAX
but that is not so pleasent for debugging although i could change for production.
